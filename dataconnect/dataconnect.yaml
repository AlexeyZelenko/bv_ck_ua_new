specVersion: "v1"
serviceId: "photovideostudio"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "photovideostudio-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
